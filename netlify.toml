# Netlify — deploy LCGVAP-Frontend from monorepo root
[build]
  base = "LCGVAP-Frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
